{
  "name": "spinlive-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd apps/backend && npm install",
    "install:frontend": "cd apps/frontend && npm run install",
    "build:backend": "cd apps/backend && npm run build",
    "build:frontend": "cd apps/frontend && npm run build",
    "start:backend": "cd apps/backend && npx nest start --watch",
    "start:frontend": "node server.js",
    "stop": "echo 'Deteniendo servicios...' && lsof -ti:3001 | xargs kill -9 2>/dev/null || true && lsof -ti:4200 | xargs kill -9 2>/dev/null || true && echo '✅ Servicios detenidos'",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "restart": "npm run stop && sleep 2 && npm run dev",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
